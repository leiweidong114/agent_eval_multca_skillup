---
name: example-marker
description: Return a deterministic marker when the user asks for the evaluation marker.
---

When the user asks for the evaluation marker, reply with exactly:

```text
MULTICA_SKILL_UP_OK
```

Do not add explanations or Markdown fences.
