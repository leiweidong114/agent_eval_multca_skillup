--- a/src/main/java/com/agent/eval/schematic/SchematicDataController.java
+++ b/src/main/java/com/agent/eval/schematic/SchematicDataController.java
@@
-    private static final List<String> REQUIRED_INSERT_FIELDS = List.of(
-            "uuid", "status", "createUser", "createTime", "checkType", "checkMessage",
-            "userName", "hscopeProjectId", "boardNum", "sessionId", "resultText");
@@
     @PostMapping("/insert")
-    public Map<String, Object> insert(
-            @RequestParam(defaultValue = CANONICAL_COLLECTION) String collectionName,
-            @RequestBody Map<String, Object> payload) {
+    public Map<String, Object> insert(@RequestBody Map<String, Object> payload) {
+        String collectionName = String.valueOf(payload.getOrDefault("collectionName", "")).trim();
         validateCollection(collectionName);
-        List<String> missing = REQUIRED_INSERT_FIELDS.stream()
-                .filter(field -> payload.get(field) == null || String.valueOf(payload.get(field)).isBlank())
-                .toList();
-        if (!missing.isEmpty()) {
-            throw new ResponseStatusException(
-                    HttpStatus.BAD_REQUEST,
-                    "missing required fields: " + String.join(", ", missing));
+        if (!(payload.get("documents") instanceof List<?> documents) || documents.isEmpty()) {
+            throw new ResponseStatusException(HttpStatus.BAD_REQUEST, "documents must be a non-empty array");
         }
 
-        Document document = new Document();
-        payload.forEach((key, value) -> {
-            if (!"_id".equals(key)) {
-                document.put(key, value);
+        List<Map<String, Object>> insertedRecords = new ArrayList<>();
+        for (Object item : documents) {
+            if (!(item instanceof Map<?, ?> source)) {
+                throw new ResponseStatusException(HttpStatus.BAD_REQUEST, "each documents item must be an object");
             }
-        });
-        document.put("createTime", parseCreateTime(payload.get("createTime")));
-        Document inserted = mongoTemplate.insert(document, collectionName);
+            Document document = new Document();
+            source.forEach((key, value) -> {
+                if (!"_id".equals(String.valueOf(key))) {
+                    document.put(String.valueOf(key), value);
+                }
+            });
+            if (document.get("createTime") != null) {
+                document.put("createTime", parseCreateTime(document.get("createTime")));
+            }
+            insertedRecords.add(normalizeDocument(mongoTemplate.insert(document, collectionName)));
+        }
 
         Map<String, Object> response = new LinkedHashMap<>();
         response.put("status", "inserted");
         response.put("collectionName", collectionName);
-        response.put("record", normalizeDocument(inserted));
+        response.put("insertedCount", insertedRecords.size());
+        response.put("documents", insertedRecords);
         return response;
     }
