diff --git a/internal/agent/custom.go b/internal/agent/custom.go
index 1dff68c..95d686e 100644
--- a/internal/agent/custom.go
+++ b/internal/agent/custom.go
@@ -18,6 +18,7 @@ import (
 
 	"github.com/alibaba/skill-up/internal/customengine"
 	"github.com/alibaba/skill-up/internal/logging"
+	"github.com/alibaba/skill-up/internal/platform"
 	"github.com/alibaba/skill-up/internal/runtime"
 	"github.com/alibaba/skill-up/pkg/transcript"
 )
@@ -268,8 +269,18 @@ func (a *CustomAgent) clearStaleOutputFile(ctx context.Context, rt Runtime, outp
 		return false
 	}
 	outputFile = safe
-	q := shellQuote(outputFile)
-	cmd := "if [ -e " + q + " ]; then rm -f -- " + q + " && printf %s " + shellQuote(customStaleClearedMarker) + "; fi"
+	quoter, err := rt.Shell().Quoter()
+	if err != nil {
+		logging.WarnContextf(ctx, "CustomAgent: cannot select shell quoter for output cleanup: %v", err)
+		return false
+	}
+	q := quoter(outputFile)
+	var cmd string
+	if rt.Shell().Family == platform.ShellCmd {
+		cmd = "if exist " + q + " (del /f /q " + q + " && echo " + customStaleClearedMarker + ")"
+	} else {
+		cmd = "if [ -e " + q + " ]; then rm -f -- " + q + " && printf %s " + quoter(customStaleClearedMarker) + "; fi"
+	}
 	result, err := rt.Exec(ctx, cmd, ExecOptions{})
 	if err != nil {
 		logging.DebugContextf(ctx, "CustomAgent: could not clear stale output file %s: %v", outputFile, err)
@@ -288,7 +299,11 @@ func (a *CustomAgent) buildLocalExec(ctx context.Context, rt Runtime, opts ExecO
 	if a.containsAPIKey(command) {
 		return "", ExecOptions{}, errSecretInCommand("local.command")
 	}
-	parts := []string{shellQuote(command)}
+	quoter, err := rt.Shell().Quoter()
+	if err != nil {
+		return "", ExecOptions{}, fmt.Errorf("select local command quoter: %w", err)
+	}
+	parts := []string{quoter(command)}
 	for i, raw := range local.Args {
 		arg, rErr := renderTemplate(raw, vars)
 		if rErr != nil {
@@ -297,7 +312,7 @@ func (a *CustomAgent) buildLocalExec(ctx context.Context, rt Runtime, opts ExecO
 		if a.containsAPIKey(arg) {
 			return "", ExecOptions{}, errSecretInCommand(fmt.Sprintf("local.args[%d]", i))
 		}
-		parts = append(parts, shellQuote(arg))
+		parts = append(parts, quoter(arg))
 	}
 
 	cwd := rt.Workspace()
diff --git a/internal/evaluator/artifacts_collect.go b/internal/evaluator/artifacts_collect.go
index 285043f..5b96812 100644
--- a/internal/evaluator/artifacts_collect.go
+++ b/internal/evaluator/artifacts_collect.go
@@ -11,6 +11,7 @@ import (
 
 	"github.com/alibaba/skill-up/internal/config"
 	"github.com/alibaba/skill-up/internal/logging"
+	"github.com/alibaba/skill-up/internal/platform"
 	"github.com/alibaba/skill-up/internal/runtime"
 )
 
@@ -114,7 +115,11 @@ func (e *defaultEvaluator) collectGlobArtifacts(ctx context.Context, rt runtime.
 // dotfile paths, so a broad glob like "**" would otherwise sweep the entire VCS
 // object store into the artifacts — framework noise, not agent output.
 func listWorkspaceFiles(ctx context.Context, rt runtime.Runtime) ([]string, error) {
-	result, err := rt.Exec(ctx, "find . -type f -not -path './.git/*'", runtime.ExecOptions{Cwd: rt.Workspace()})
+	command := "find . -type f -not -path './.git/*'"
+	if rt.Shell().Family == platform.ShellCmd {
+		command = "dir /s /b /a-d"
+	}
+	result, err := rt.Exec(ctx, command, runtime.ExecOptions{Cwd: rt.Workspace()})
 	if err != nil {
 		return nil, err
 	}
@@ -125,7 +130,15 @@ func listWorkspaceFiles(ctx context.Context, rt runtime.Runtime) ([]string, erro
 			continue
 		}
 		rel := strings.TrimPrefix(line, "./")
-		if rel == "" {
+		if rt.Shell().Family == platform.ShellCmd {
+			var relErr error
+			rel, relErr = filepath.Rel(rt.Workspace(), line)
+			if relErr != nil || rel == "." || strings.HasPrefix(rel, "..") {
+				continue
+			}
+			rel = filepath.ToSlash(rel)
+		}
+		if rel == "" || rel == ".git" || strings.HasPrefix(rel, ".git/") {
 			continue
 		}
 		files = append(files, rel)
